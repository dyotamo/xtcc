<% if model.errors.any? %>
  <div id="error_explanation">
    <p class="alert alert-danger"><%= pluralize(model.errors.count, "error") %> prohibited this from being saved:</p>
    <ul class="text-danger">
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
