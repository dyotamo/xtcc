<% title @course.name %>
<div class="panel panel-default">
  <div class="panel-heading">
    Monografias sobre <strong><%= @course.name %></strong>
    <%= link_to 'Edit', edit_college_path(@course), class: "btn btn-primary btn-xs" %>
    <%= link_to 'Wipe', @course, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-xs" %>
  </div>
  <div class="panel-body">
    <% if @monos.empty? %>
      <em>(Sem monografias)</em>
    <% end %>
    <ul>
      <% @monos.each do |mono|%>
        <li><%= link_to mono.title, mono %></li>
      <% end %>
    </ul>
  </div>
  <div style="text-align: center;">
    <%= will_paginate @monos, renderer: BootstrapPagination::Rails %>
  </div>
</div>
